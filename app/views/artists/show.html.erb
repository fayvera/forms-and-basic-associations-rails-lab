<h1>Artists#show</h1>
<p>Find me in app/views/artists/show.html.erb</p>

<h3>Name: <%= @artist.name %> <h3>
    <ul>
        <% @artist.songs.each do |song| %>
           <li> <%= link_to song.name, @artist.song_path(song) %></li>
        <% end %>
    </ul
</br>
<%= link_to "All Artists", artists_path %>